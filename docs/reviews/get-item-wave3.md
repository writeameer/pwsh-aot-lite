# Review: Get-Item Wave 3 direct physical-item slice

Date: `2026-09-23`
Claim reviewed: `Integrated native subset: macOS-arm64 captured-root direct physical Path lookup returns exactly one non-link file or directory with the shared upstream Unix default view.`
Upstream commit: `pinned .upstream/PowerShell reference; Navigation.cs GetItemCommand`

## Evidence

- Source/provenance: `.upstream/PowerShell/src/Microsoft.PowerShell.Commands.Management/commands/management/Navigation.cs:1847-2003`; generated `GeneratedCmdletPorts.GetItem`; `FileSystemProvider.cs:221-230,1253-1285`; `FileSystem_format_ps1xml.cs:46-69`; `TypeTable_Types_Ps1Xml.cs:9090-9127`; `CorePsPlatform.cs:665-748`.
- Target boundary: `PhysicalChildItemCatalog.cs` (`GetDirectPhysicalItem`, shared validation/acquisition, `PhysicalItemPresentation`, and `GetItemCmdlet`), `HostSubstrate.cs`, `Pipeline.cs`, `HelpCatalog.cs`, and `PipelineValueAdapter.cs`.
- Build/test: `dotnet build -c Release --no-restore -v:q` and `dotnet run -c Release --no-build -- --self-test` passed. Focused self-tests cover generated metadata, positional/named `Path`, direct file/directory cardinality, no nested-child enumeration, presentation identity, cancellation, provider rejection/span, unsupported `-LP`, missing `Path`, and ancestor-link rejection. `Pipeline.cs` also has a checked plain-terminal snapshot for the intentional missing-Path `AOT6211` boundary, including its `getitem-missing-path.ps1:1:1-1:9` command span.
- Native AOT evidence: fresh `dotnet publish -c Release -r osx-arm64 --self-contained true -o artifacts/osx-arm64-get-item-release-20260923 --no-restore -v:q` passed; the tested binary was `artifacts/osx-arm64-get-item-release-20260923/PwshAotLite`, built from the `275b920` base plus the reviewed Get-Item working-tree changes, with SHA-256 `43a405f7104498d5dd010f8bbeb23dbc8c933889e00e62f3c9395b4713b36438`. It passed `--self-test`, direct file/directory smoke, missing-Path (`AOT6211`), unsupported-parameter, wildcard, and provider diagnostics.
- Differential parser evidence: no parser/lowerer change; `Test-ParserReuseGuard.ps1` and `Export-PwshParserBaseline.ps1 -Verify` both passed (32 fixtures). `Export-Phase10Campaign.ps1 -Verify` also passed (290 declarations / 288 logical commands).
- Upstream reuse matrix: [Get-Item port note](../cmdlets/get-item.md).
- Runtime oracle: `tools/Test-GetItemFormatCompatibility.ps1` controls a direct file, direct directory/nested child, and symlinked ancestor; it compares full PlainText default views with ANSI/newline transport normalization only. It used installed stock `pwsh` `7.6.6` and the identified fresh native artifact above; it also proved that the nested child is absent and an ancestor link yields `AOT6205` with no table output.
- Approved AOT replacement exceptions: `getitem-direct-physical-provider-boundary`, `getitem-shared-closed-physical-record`, `getitem-unix-format-static-extraction`, and the reused `childitem-macos-unixstat-bridge`; all remain fail closed.

## Independent verdicts

| Persona | PASS / BLOCK | Evidence-backed finding | Resolution / accepted variance |
| --- | --- | --- | --- |
| Upstream Grammar Steward | **PASS** | `git diff --name-only` contains no parser, lexer, AST lowerer, grammar fixture, or parser-baseline change. `AotCmdletRegistry.ParseSource` still delegates to `UpstreamAstPipelineLowerer.BindSingleCommand`; the new adapter receives only the resulting `CommandInvocation`/source spans and never reparses command text. Fresh independent `Test-ParserReuseGuard.ps1` and `Export-PwshParserBaseline.ps1 -Verify` passed (32 fixtures). | The source `Path` declaration has `ValueFromPipeline` and `ValueFromPipelineByPropertyName`, but that is not silently approximated: the existing structural lowerer rejects `Get-ChildItem . | Get-Item` with source-spanned `AOT1001` because `GetItemCmdlet` does not opt into `IAotPipelineInputCmdlet`. Pipeline binding stays an explicit future slice. |
| Native AOT Boundary Sentinel | **PASS** | `GetDirectPhysicalItem` calls the shared `TryResolveDirectPhysicalPath` and `TryDescribe` paths verbatim; it does not enumerate a directory or add a second stat/open path. Acquisition remains the reviewed all-components descriptor walk: lexical `Path.GetFullPath`, then parent-relative `fstatat(..., AT_SYMLINK_NOFOLLOW)` and `openat(..., O_NOFOLLOW)`, followed by descriptor `fstat`. No `FileInfo`, provider dispatch, `PSObject`, reflection, or path-based filesystem fallback was added. `PhysicalItemPresentation` is a single immutable owner of the already source-attributed Unix table and both cmdlets reuse the same layout object. Independent evidence passed: managed build/self-test, parser guard/baseline, fresh Native AOT publish at `artifacts/osx-arm64-getitem-aot-review`, native self-test, strict file/directory/no-enumeration/ancestor-link oracle, and a separate final-link probe (`AOT6205`). | The explicit `AOT6211` adapter branch is accepted: generated metadata still declares source-mandatory `Path`, while the noninteractive static runner intentionally has no mandatory-parameter prompt; missing `Path` fails closed rather than becoming an invented empty-result behavior. Pass is limited to macOS-arm64 direct physical paths and the documented static display subset. |
| Static Data-Plane & Binder Guardian | **PASS** | `GetItemCmdlet` derives its sole executable descriptor directly from `GeneratedCmdletPorts.GetItem.CreateAotDescriptor("Path")`; generated metadata preserves the source mandatory, position-zero `Path` contract, while no handwritten parameter, alias, or alternate binder was introduced. The shared binder admits named and positional `Path`, rejects `-LP` through `AOT2002`, and the adapter fails closed with `AOT6211` when the noninteractive runner cannot supply source-mandatory `Path`. The port returns the existing finite `PhysicalChildItemRecord` via `PhysicalItemPresentation.ToRecord`; it crosses the generic pipeline only through the already explicit `PipelineValueAdapter` case and its declared record fields. No `PSObject`, reflection, arbitrary property bag, or object conversion path was added. Managed build/self-test passed, as did focused positional/named, `-LP`, missing-Path, direct file/directory, no-enumeration, and source-span probes. | Projection remains at the established closed `AotRecord` boundary, and typed pipeline input remains deliberately unimplemented. The adapter must not be changed to `IAotPipelineInputCmdlet` until a separate reviewed contract can faithfully implement source `Path` pipeline/by-property-name binding. |
| Compatibility Proof Adversary | **PASS** | Re-review independently verified the repaired evidence chain. `getitem-mandatory-path-noninteractive` is now a durable variance with its second-pass boundary; `Pipeline.cs` snapshots the full plain terminal `AOT6211` rendering and `getitem-missing-path.ps1:1:1-1:9` command span. The recorded fresh artifact exists and its SHA-256 independently matches `43a405f7104498d5dd010f8bbeb23dbc8c933889e00e62f3c9395b4713b36438`. Managed build/self-test, parser guard, parser baseline (32 fixtures), campaign verification (290 declarations / 288 logical commands), native `--self-test`, and the controlled oracle all passed again. The oracle uses installed stock `pwsh` `7.6.6` and allows only ANSI/newline transport normalization: direct file and directory views matched; the nested child was absent; a symlinked ancestor failed closed with `AOT6205`. Earlier focused probes remain valid for source-spanned `AOT6211`, `AOT2002`, `AOT6201`, and `AOT6202`, with no raw exception. | PASS applies only to the documented macOS-arm64 captured-root direct-physical `Path` subset and fixed upstream-derived Unix table. Provider drives, literal/wildcard paths, hidden/link behavior, dynamic parameters, pipeline binding, and interactive prompting remain explicitly deferred or fail closed. |
| Upstream Reuse & Format-Contract Reviewer | **PASS** | The evidence correction is complete: the port note and this ledger identify the stock oracle as `pwsh` `7.6.6`, and identify the recorded `osx-arm64-get-item` target source plus SHA-256. The implementation has one shared `PhysicalItemPresentation` owner—both `GetChildItemCmdlet` and `GetItemCmdlet` reference its identical table, columns, and record projection—and `GetDirectPhysicalItem` calls `TryDescribe` directly rather than child enumeration. No fields, headers, widths, order, grouping, or time rule are invented: the source `childrenWithUnixStat` table is statically extracted. I independently re-published the current tree to `artifacts/osx-arm64-getitem-reuse-rereview/PwshAotLite` (SHA-256 `43a405f7104498d5dd010f8bbeb23dbc8c933889e00e62f3c9395b4713b36438`), passed native `--self-test`, and passed the exact stock/native file, directory, no-nested-child, and ancestor-link oracle. The script permits only ANSI/newline transport normalization. | Accepted bounded variance: direct non-link macOS-arm64 physical items use the shared closed record/static Unix view; provider/type-table discovery, links, and deferred parameters remain explicit fail-closed boundaries. |

## Outcome

`integrated-native-subset` — all required independent verdicts pass and fresh
Native AOT evidence is recorded. This is not a general provider-engine,
pipeline-binding, wildcard, or interactive-host compatibility claim.
