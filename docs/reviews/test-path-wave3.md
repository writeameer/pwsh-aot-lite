# Review: Test-Path Wave 3 direct physical-probe slice

Date: `2026-09-22`  
Claim reviewed: `Integrated bounded direct physical Path probe with static Any/Container/Leaf Boolean result.`
Upstream commit: `pinned .upstream/PowerShell reference; TestPathCommand`

## Evidence

- Source/provenance: `.upstream/PowerShell/src/Microsoft.PowerShell.Commands.Management/commands/management/TestPathCommand.cs`; generated `GeneratedCmdletPorts.TestPath`.
- Target boundary: `PhysicalChildItemCatalog.cs` (`ProbeDirectPhysicalItem`, `PhysicalItemProbeResult`, `TestPathCmdlet`), `Pipeline.cs` (`BooleanRecord`), `PipelineValueAdapter.cs`, and `HelpCatalog.cs`.
- Build/test: `dotnet build -c Release --no-restore -v:q` and `dotnet run -c Release --no-build -- --self-test` passed. Focused self-tests cover generated metadata, `Path`/`-Type` binding, Any/Container/Leaf file/directory behavior, missing/whitespace false, the direct closed probe, provider source span/rejection, ancestor-link rejection, inactive LiteralPath/IsValid, and invalid static PathType.
- Native AOT evidence: post-commit fresh `dotnet publish -c Release -r osx-arm64 --self-contained true -o artifacts/osx-arm64-test-path-final-20260922 --no-restore -v:q` passed for implementation commit `c473b377e035d992300970e75d62ee10bf4a7793`; `artifacts/osx-arm64-test-path-final-20260922/PwshAotLite --self-test` and `tools/Test-TestPathCompatibility.ps1` passed. The fresh executable SHA-256 is `238d357f39a8ba140d770aeb6d91db119df26a0df66b9871e1e0c31ed0d4205b`.
- Differential parser evidence: `Test-ParserReuseGuard.ps1` and `Export-PwshParserBaseline.ps1 -Verify` passed with 33 fixtures, including the new syntax-only Test-Path fixture. `Export-Phase10Campaign.ps1 -Verify`, `Export-Phase10BatchQueue.ps1 -Verify`, and `git diff --check` passed.
- Upstream reuse matrix: [Test-Path port note](../cmdlets/test-path.md).
- Runtime oracle comparison: `tools/Test-TestPathCompatibility.ps1` used installed `pwsh` `7.6.6` and the fresh native artifact above. It compared direct ordinary file/directory/missing/whitespace input under Any/Container/Leaf with exact scalar Boolean output (no normalization), and separately asserted an ancestor link emits `AOT6205` and no Boolean.
- Approved AOT replacement exceptions: `testpath-direct-physical-probe`, `testpath-closed-pathtype-and-boolean`.

## Independent verdicts

| Persona | PASS / BLOCK | Evidence-backed finding | Resolution / accepted variance |
| --- | --- | --- | --- |
| Upstream Grammar Steward | **PASS** | `git diff --name-status 78e5b1083112a127fd335b9b79608184abe2a653..c473b377e035d992300970e75d62ee10bf4a7793` changes no parser, AST, lowerer, grammar, or parser-reuse-guard implementation. The sole grammar addition is the syntax-only `33-test-path-direct-probe.ps1` fixture/baseline. I independently reran `Test-ParserReuseGuard.ps1` and `Export-PwshParserBaseline.ps1 -Verify`; the guard passed and all 33 upstream-parser differential baselines verified. | `Test-Path` execution is admitted only after the existing upstream parser/AST path; no second grammar, tokenization, parser, or custom command syntax exists. |
| Native AOT Boundary Sentinel | **PASS** | `ProbeDirectPhysicalItem` shares the catalog's single `TryResolveDirectPhysicalPath` gate, then calls only `ProbeCanonicalDirectPhysicalItem` and `MacOsPhysicalMetadata.TryAcquireNoFollow`. The latter retains the reviewed all-components `/`-descriptor walk, `fstatat(..., AT_SYMLINK_NOFOLLOW)`, `openat(..., O_NOFOLLOW)`, and final descriptor `fstat`. The probe does **not** call `GetDirectPhysicalItem` or `TryDescribe`, enumerate, create Unix display metadata, construct an item record, use `File.Exists`/`Directory.Exists`, `FileInfo`/`DirectoryInfo`, provider dispatch, reflection, or a path-based fallback. Its closed result is sound: no-follow missing is the only `Missing`/no-error result; unsupported, provider/wildcard, link, hidden, access, and I/O boundaries emit their existing source-spanned diagnostic and return `Rejected`; `TestPathCmdlet` emits no Boolean for `Rejected`. I independently built the current working tree, ran `--self-test`, ran `git diff --check`, then ran the recorded fresh `osx-arm64-test-path` native executable's `--self-test` and exact scalar/no-follow oracle successfully. | Approved only for the documented macOS-arm64 captured-root, direct ordinary file/directory subset. `TryAcquireNoFollow` rejects non-regular/non-directory items before a `Found` result; hidden and link paths remain visible rejected boundaries, never false. |
| Static Data-Plane & Binder Guardian | **PASS** | The target calls only `GeneratedCmdletPorts.TestPath.CreateAotDescriptor("Path", "PathType")`; the generator-produced source contract retains source `string[] Path`, mandatory position zero, and `PathType` alias `Type`. `TestPathCmdlet` has one finite case-insensitive `Any`/`Container`/`Leaf` parser, emits only `BooleanRecord`, and has no pipeline/object binder or discovery path. `BooleanRecord` is a finite typed pipeline record registered explicitly in `PipelineValueAdapter` as a Boolean field and rendered through the existing prose scalar route—no `PSObject`, ETS, reflection, or table/view imitation. Self-tests cover generated metadata, direct binding, invalid static type, inactive `LiteralPath`/`IsValid`, omitted required `Path`, and mixed value behavior. | Approved for the explicit two-parameter generated subset only. Unselected generated parameters remain inactive and fail closed through the shared binder; numeric/abbreviated enum coercion and pipeline/property binding are deliberate deferrals. |
| Compatibility Proof Adversary | **PASS** | I independently reran `artifacts/osx-arm64-test-path-final-20260922/PwshAotLite` from implementation commit `c473b377e035d992300970e75d62ee10bf4a7793` (SHA-256 `238d357f39a8ba140d770aeb6d91db119df26a0df66b9871e1e0c31ed0d4205b`). Its `--self-test` and the strict native-versus-stock `pwsh` `7.6.6` scalar oracle both pass: direct file/directory/missing/whitespace values under `Any`/`Container`/`Leaf` have exact Boolean output with no normalization, and ancestor-link rejection emits `AOT6205` without a Boolean. Managed regression evidence additionally snapshots omitted-`Path` `AOT6211` with its Test-Path source span/plain terminal text and proves mixed provider/wildcard/link values retain their own error span while admitted siblings retain Boolean order/cardinality. | Approved only for the documented macOS-arm64 direct physical `Path` and exact static `PathType` vocabulary. Provider/wildcard/link behavior remains explicit fail-closed variance, not a claimed source-compatible provider engine. |
| Upstream Reuse & Format-Contract Reviewer | **PASS** | The cmdlet note’s matrix accurately traces source metadata (`TestPathCommand.cs:45-137`), provider lifecycle (`:157-240`), the `Any`/`Container`/`Leaf` decision (`:213-224`), and scalar `WriteObject(bool)` (`:241`). The generated metadata is reused; the unavoidable `SessionState`/provider path is replaced by the existing captured-root catalog with the narrower shared `Found`/`Missing`/`Rejected` fact rather than a second filesystem API. The explicit variance ledger correctly records the target’s fail-closed provider/wildcard/link behavior and direct finite Boolean projection. The native oracle compares the source scalar output exactly—no normalized formatting and no invented PowerShell view. | The reuse decision is approved only for the documented direct physical macOS-arm64 slice. It does not claim provider-engine behavior, dynamic parameters, `PSObject`, or formatting compatibility beyond exact direct Boolean scalar output. |

## Outcome

`integrated bounded slice` — managed, parser, Native AOT, oracle, and all
independent review gates passed before integration. The integrated claim does
not expand the documented compatibility boundary.
