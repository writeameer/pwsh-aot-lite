# Review: Resolve-Path Wave 3 direct physical-resolution slice

Date: `2026-09-23`  
Claim under review: `Generated Path-only adapter resolves existing direct physical paths into a closed canonical Path record.`

## Evidence to review

- Source: `.upstream/PowerShell/src/Microsoft.PowerShell.Commands.Management/commands/management/ResolvePathCommand.cs`; generated `GeneratedCmdletPorts.ResolvePath`.
- Target: `PhysicalChildItemCatalog.cs`, `Pipeline.cs`, `PipelineValueAdapter.cs`, and `AotLanguageCore.cs` layout data only.
- Closed seam: `IPhysicalChildItemCatalog.ResolveExistingDirectPhysicalPath` returns exactly `Resolved(DirectPhysicalPathRecord)`, `Missing`, or `Rejected` through the one metadata-free no-follow acquisition primitive.
- Grammar evidence: fixture/baseline `34-resolve-path-direct-physical` and parser reuse guard.
- Compatibility evidence: `tools/Test-ResolvePathCompatibility.ps1` compares stock PlainText and native direct file/directory rendering exactly, including leading blank; it also asserts Missing continuation, empty vs whitespace, captured-root behavior, `-Relative` rejection, and ancestor-link no-follow.
- Working-branch verification: managed Release build/self-test, parser reuse guard,
  34-fixture parser baseline, Phase 10 campaign/queue verification, and
  `git diff --check` passed. Final immutable implementation source commit
  `58060bfea4a872f6b7070a1469d0746218ff561f` was freshly published as
  `artifacts/osx-arm64-resolve-path-release-20260923-0833/PwshAotLite`; it
  passed `--self-test` and the strict oracle; SHA-256
  `f6a7de65202c85b1c37967532238708decb316aea5c87775bd31e7d5e9d0bff9`.

## Independent verdicts

| Persona | PASS / BLOCK | Evidence-backed finding | Resolution / accepted variance |
| --- | --- | --- | --- |
| Upstream Grammar Steward | **PASS** | The command is registered as a static adapter only; the diff adds no lexer, grammar, parser, AST, or lowerer code. The new fixture is intentionally a parser-differential input (positional/named `Path` plus upstream-declared deferred syntax), rather than a second grammar. I independently ran `tools/Test-ParserReuseGuard.ps1` and `tools/Export-PwshParserBaseline.ps1 -Verify`: the upstream-parser guard passed and all 34 fixtures verified. | Parser/AST reuse is intact. Deferred syntax remains accepted by the upstream parser and rejected later by the shared static binder, rather than being removed from or reimplemented in a grammar. |
| Native AOT Boundary Sentinel | **PASS** | `ResolvePathCmdlet` invokes only `IPhysicalChildItemCatalog.ResolveExistingDirectPhysicalPath`; it has no `GetDirectPhysicalItem`, `TryDescribe`, `File.Exists`/`Directory.Exists`, `FileInfo`/`DirectoryInfo`, enumeration, provider, or reflection escape. The catalog lexical-normalizes from its immutable captured root, then its one private `AcquireCanonicalDirectPhysicalPath` path acquires every component with the pre-existing no-follow descriptor walk and returns the closed `Found`/`Missing`/`Rejected` fact. Only the `Found` branch makes `DirectPhysicalPathRecord(Path)`; display metadata remains above this primitive in `TryDescribe` for the item commands. I independently ran the managed Release build/self-test, parser/campaign/queue guards, and diff check, then freshly published `artifacts/osx-arm64-resolvepath-aot-review/PwshAotLite` (SHA-256 `7e6fbe3bd3f521e0f07fbce4db16f1155ebdcc3440113afbcec0915228d7975e`). Its native `--self-test` and strict stock/native direct-file, direct-directory, continuation, captured-root, empty/whitespace, `-Relative` rejection, and ancestor-link no-follow oracle pass. | Approved only for the documented macOS-arm64 captured-root direct ordinary file/directory slice. This does not admit a provider engine, links, wildcards, `PathInfo`/`PSObject`, or runtime path discovery. |
| Static Data-Plane & Binder Guardian | **PASS** | `ResolvePathCmdlet` owns exactly `GeneratedCmdletPorts.ResolvePath.CreateAotDescriptor("Path")`; its self-test proves generated position-zero mandatory `Path`, positional/named binding, and binder rejection (`AOT2002`) for `-LP`, `-Relative`, and `-RelativeBasePath`. The only output record is explicit `DirectPhysicalPathRecord(Path)`, explicitly registered in `PipelineValueAdapter`; no `PathInfo`, `PSObject`, CLR fallback, or dynamic property bridge is introduced. Its one static `ResolvePathPresentation.PathTable` is retained only for a direct untransformed terminal result. | The source's pipeline/property-name, literal-path, provider, and relative contracts remain documented deferred behavior. The emitted canonical path is a typed, closed record—not fake compatibility output. |
| Compatibility Proof Adversary | **PASS** | I independently ran the managed Release build/self-test, parser/campaign/queue checks, `git diff --check`, `artifacts/osx-arm64-resolve-path-working/PwshAotLite --self-test`, and `tools/Test-ResolvePathCompatibility.ps1 -NativePwshPath artifacts/osx-arm64-resolve-path-working/PwshAotLite`; all passed. The oracle compares redirected stock `pwsh` PlainText and Native AOT stdout byte-for-byte for a controlled direct file and directory, including the observed leading/final blank lines. It also proves missing-value continuation, captured-root behavior, `-Relative` binder rejection, and ancestor-link no-follow. Manual native checks confirm source-spanned `AOT6213` for explicit empty input, `AOT6206` for literal whitespace/missing input, and `AOT2002` for deferred parameters. Stock `pwsh` likewise distinguishes empty input (binding failure) from whitespace (missing literal path); the different bounded diagnostic IDs are explicitly recorded rather than disguised as compatibility. | Approved only for the documented macOS-arm64 direct ordinary file/directory subset. `AOT6211` noninteractive omitted-`Path` and `AOT6213` explicit-empty behavior are deliberate, tested host/boundary variances; provider and relative semantics are not claimed. |
| Upstream Reuse & Format-Contract Reviewer | **PASS** | The source matrix accurately traces source metadata (`ResolvePathCommand.cs:18-105`) and its provider lifecycle (`:166-246`) to generated metadata plus the already-existing captured-root/no-follow catalog. The added `ResolveExistingDirectPhysicalPath` shares one metadata-free acquisition primitive with item lookup and `Test-Path`; it neither calls `GetDirectPhysicalItem`/`TryDescribe`, enumerates, nor reintroduces `File.Exists`/provider dispatch. `ResolvePathPresentation.PathTable` is the one reviewed place that declares the source-observed Path-only view and literal blanks; `TableWriter` merely consumes immutable layout data and has no command-name special case. The strict controlled-stock oracle passed. | Approved shared seam and attributed format data. A future command may reuse the closed resolution fact, but must not widen it into `PathInfo`, provider/session semantics, or a global formatting policy. |

## Outcome

All required independent review gates are **PASS**. The port remains on its
feature branch until the release steward records end timing, commits, merges,
pushes, and updates the campaign queue.
