# Review: upstream parser runner connection

Date: `2026-09-21`  
Claim reviewed: `The runner may execute the reviewed non-DSC structural pipeline subset through the pinned upstream parser and existing static registry.`  
Upstream commit: `1e53f6bbab4b8791eae782474d21889f9e5d6038`

## Evidence

- Source/provenance: [language extraction provenance](../../language/UPSTREAM.md),
  [parser reuse guard](../../PARSER-REUSE-GUARD.md), and
  [runner connection design](../architecture/upstream-parser-runner-integration.md).
- Build and tests: independent reviewers ran `dotnet build -c Release`,
  `dotnet run -c Release -- --self-test`, parser-reuse guard verification, and
  the language fixture comparison successfully.
- Native AOT evidence: fresh `osx-arm64` publish and `Get-Process |
  Where-Object CPU -gt 10 | Select-Object Name, Id` smoke passed with the
  documented Homebrew OpenSSL/Brotli `LIBRARY_PATH`.
- Differential parser evidence: `language/PwshAotLite.Language.csproj` compares
  all checked-in fixture token/text/extent, AST hierarchy/extent, and
  diagnostic-ID/extent baselines generated by stock pwsh.

## Independent verdicts

| Persona | PASS / BLOCK | Evidence-backed finding | Resolution / accepted variance |
| --- | --- | --- | --- |
| Upstream Grammar Steward | **PASS** | All executable and fixture source parsing reaches `Parser.ParseInput`; no second lexer or pipeline splitter remains. Four fixture baselines match stock `pwsh` tokens, flags/extents, AST hierarchy/extents, and diagnostics. | One top-level source command with optional direct-property `Where-Object` and `Select-Object` only. |
| Native AOT Boundary Sentinel | **PASS** | Native root publish/run passed; DSC, dynamic keywords, compiler/binder/remoting, and generic/array type materialization are fail-closed. | Parse-only structural subset only. |
| Static Data-Plane & Binder Guardian | **PASS** | AST lowering uses the one descriptor-backed binder; literal conversion is closed and AST-path alias/default/rejection tests pass. | Existing typed `IPipelineRecord` remains a temporary data plane. |
| Compatibility Proof Adversary | **PASS** | Refreshed native artifact passed positive pipeline, malformed-pipe `AotParseError: EmptyPipeElement`, script-block `AotUnsupportedSyntax`, and excess-stage `AotUnsupportedSyntax` assertions. | Do not widen support claims beyond the tested non-DSC structural subset. |

## Outcome

`integrated — limited structural subset` — the runner executes the documented
non-DSC source → optional direct-property `Where-Object` → optional direct
`Select-Object` subset through the pinned upstream parser and static registry.
Script blocks, arbitrary expressions, DSC, dynamic keywords, redirections,
general command pipelines, type execution, and AST execution remain explicitly
unsupported.
