# Review: Get-ChildItem Wave 3 direct-physical slice

Date: `2026-09-22`  
Claim reviewed: `Integrated bounded captured-root reviewed-macOS-arm64 direct physical default/positional/-Path immediate-child listing with the statically extracted upstream Unix default view only.`  
Upstream commit: `pinned .upstream/PowerShell reference; GetChildrenCommand.cs`

## Evidence

- Source/provenance: `.upstream/PowerShell/src/Microsoft.PowerShell.Commands.Management/commands/management/GetChildrenCommand.cs:14-55,181-237`; generated `GeneratedCmdletPorts.GetChildItem`.
- Target boundary: `PhysicalChildItemCatalog.cs`, `HostSubstrate.cs`, `AotLanguageCore.cs`, `AotTerminalEventProjector.cs`, `Pipeline.cs`, and `PipelineValueAdapter.cs`.
- Upstream format/type evidence: `.upstream/PowerShell/src/System.Management.Automation/FormatAndOutput/DefaultFormatters/FileSystem_format_ps1xml.cs:46-65` (`childrenWithUnixStat` headers, group, alignment, widths, and `LastWriteTime` script format); `.upstream/PowerShell/src/System.Management.Automation/engine/TypeTable_Types_Ps1Xml.cs:9090-9127` (UnixMode/User/Group/Size); `.upstream/PowerShell/src/System.Management.Automation/CoreCLR/CorePsPlatform.cs:665-748` (mode and UID/GID name semantics); `.upstream/PowerShell/src/System.Management.Automation/namespaces/FileSystemProvider.cs:1644-1674` (directories-first/current-culture case-insensitive ordering).
- Fixtures: `tests/grammar/fixtures/32-get-childitem-direct-paths.ps1` plus matching checked baseline; `SelfTest` direct catalog fixture covers default captured dot, exact file, source ordering, Unix facts, static view shape, hidden omission, provider/wildcard rejection with source span, descriptor rejection, direct-link rejection, direct-catalog cancellation, and the Darwin arm64 gate.
- Runtime oracle: `tools/Test-GetChildItemFormatCompatibility.ps1` creates a three-row reviewed-macOS-arm64 direct physical fixture, captures raw normal/default and native/default views, and compares the full group/header/row geometry. Normalization is limited to ANSI/newline transport and a terminal trailing blank only; inter-column whitespace is asserted exactly.
- Build/test and Native AOT evidence: reviewer ran `dotnet build -c Release --no-restore -v:q`, managed `--self-test`, fresh `dotnet publish -c Release -r osx-arm64 --self-contained true -o artifacts/osx-arm64-gci-reuse-review --no-restore -v:q`, fresh native `--self-test`, and `Test-GetChildItemFormatCompatibility.ps1` against that artifact; all passed. The controlled normal/native default-view oracle compared headers, grouping, values, and ordering with only documented presentation-whitespace normalization. A pre-existing `artifacts/osx-arm64-get-childitem` binary was demonstrably stale (old invented table) and was not used as evidence.

## Independent verdicts

| Persona | PASS / BLOCK | Evidence-backed finding | Resolution / accepted variance |
| --- | --- | --- | --- |
| Upstream Grammar Steward | **PASS** | Reviewed the only grammar-facing addition: `32-get-childitem-direct-paths.ps1` uses pre-existing PowerShell command, parameter, pipeline, comma-array, and newline syntax. Its checked-in baseline was generated from the stock `pwsh` parser and verified with `Export-PwshParserBaseline.ps1 -Verify` (32 fixtures); its SHA-256 matches the baseline. `Test-ParserReuseGuard.ps1` also passed. The production diff touches static terminal-layout propagation only (`AotLanguageCore.cs`); it adds no tokenizer/parser/AST/lowerer rule, grammar facade, syntax reinterpretation, or source-span logic. | Fixture coverage may exercise already-supported parse structures while the command binder deliberately rejects deferred `LiteralPath`/`Recurse`; parsing them is not a support claim. No grammar compatibility variance is introduced. |
| Native AOT Boundary Sentinel | **PASS** | Independently re-reviewed the repair. `Path.GetFullPath` is lexical only; every non-root component is then checked through the already-open parent descriptor with `fstatat(..., AT_SYMLINK_NOFOLLOW)` and acquired with parent-relative `openat(..., O_NOFOLLOW)`, followed by descriptor `fstat`. The literal `/` anchor is opened as a directory; no later path-based BCL access, `FileInfo`, or link-following reopen remains. A component replacement race to a link fails at the component's `O_NOFOLLOW` acquisition. Directory child metadata remains descriptor-relative through `fdopendir`/`fstatat(..., AT_SYMLINK_NOFOLLOW)`. Cancellation checks remain before physical work, around enumeration/statting/record creation, and inside sorting. Fresh evidence passed: managed build/self-test (including `AOT6205` ordinary-final-file-behind-symlinked-ancestor fixture), parser/campaign gates, fresh `osx-arm64` Native AOT publish at `artifacts/osx-arm64-gci-aot-ancestor-rereview`, native `--self-test`, and strict normal-pwsh table plus ancestor-link fail-closed oracle. | macOS arm64 only; the documented all-components direct-path no-follow contract is now satisfied. |
| Static Data-Plane & Binder Guardian | **PASS** | Independently traced the active descriptor to `GeneratedCmdletPorts.GetChildItem.CreateAotDescriptor("Path")`; `Path` retains generated array/position-zero metadata while every non-admitted source parameter (including `-LP` and `-Recurse`) fails in the sole shared binder as `AOT2002`. The port does not opt into `IAotPipelineInputCmdlet`: despite upstream `Path` pipeline metadata, a downstream `Get-ChildItem` stage is rejected by the structural lowerer as `AOT1001`, so no string/object coercion or fake pipeline binding occurs. `PhysicalChildItemRecord` is an explicit finite `IPipelineRecord` case in `PipelineValueAdapter`; it crosses as an `AotRecord` with typed `Null`/integer/Boolean/DateTime/string fields and no `PSObject`, reflection, dynamic dispatch, or arbitrary-property fallback. The source-attributed table descriptor is applied only when the direct record shape is untouched; `Select-Object` rebuilds a closed shape and cannot retain the filesystem view. Fresh focused evidence passed: Release managed build and `--self-test`; parser-reuse guard; 32-fixture parser baseline verification; Phase 10 manifest verification; direct probes for unsupported `-LP`/`-Recurse`, rejected downstream pipeline input, and closed `Get-ChildItem | Select-Object Name, IsDirectory, Size` projection. | Direct physical `Path` only; source pipeline binding, `LiteralPath`, and all remaining provider parameters stay explicitly deferred/fail closed. |
| Diagnostic Experience Guardian | **PASS** | The diagnostic review passed the strict table and focused negative probes (`AOT6206`, `AOT2002`, and `AOT6202`) without raw exceptions. Its earlier `artifacts/osx-arm64-gci-compat-rereview` binary predates the final ancestor-link correction and is excluded from final native evidence; the current native AOT boundary and compatibility reviews independently rebuilt from the corrected source and passed the strict table plus ancestor-link oracle. The source-attributed `childrenWithUnixStat` layout uses its one-space TableControl separator, source widths/alignments, and only trims final-cell tail padding. | Final timing is recorded only with verified integration; the current diagnostic claim remains bounded to the documented direct physical slice. |
| Compatibility Proof Adversary | **PASS** | Independently published the current candidate to `artifacts/osx-arm64-gci-compat-adversary`, then passed native `--self-test` and the strict `Test-GetChildItemFormatCompatibility.ps1` controlled fixture oracle. The oracle compared the normal `pwsh` plain-text default table to the native direct `-Path` table without semantic or geometry normalization (ANSI/newline transport and terminal trailing newline only), and separately proved that a normal target beneath a symlinked ancestor fails closed with `AOT6205`. Direct default captured-root listing, positional `Path`, named `-Path`, and comma-array paths each emitted the documented closed physical result; `-LiteralPath` and `-Recurse` rejected at binding with `AOT2002`, wildcard `Path` with `AOT6202`, and provider-qualified `Path` with `AOT6201`. A previously existing `osx-arm64-gci-compat-rereview` binary failed the ancestor-link probe and was therefore treated as stale and excluded from evidence. | Pass applies only to the documented macOS-arm64 captured-root/direct physical immediate-child slice and fixed untransformed Unix table. It does not imply provider, drive, wildcard, literal-path, recurse, hidden/link, pipeline-input, or general formatting compatibility. |
| Upstream reuse & format-contract reviewer | PASS | Independently traced the active command declaration, provider ordering, Unix type data, `childrenWithUnixStat` view, and `CommonStat` mode/name semantics at the pinned paths above. The target retains generated metadata; copies the admitted ordering and mode algorithm mechanically; extracts the one fixed source view (six columns, header/order/alignment/minimum widths, parent grouping, local time format); and confines it to direct, untransformed results. The required fresh Native AOT controlled oracle passed without semantic normalization. | The documented provider/session/ETS/script-block boundary justifies the one immutable physical catalog plus fixed layout; unsupported paths and dynamic display behavior remain fail-closed/deferred. The obsolete pre-extraction table exists only in a stale artifact and is not source behavior. |

## Outcome

`integrated` — all required independent verdicts are **PASS**. The claim is
limited to the reviewed macOS-arm64 captured-root direct-physical
default/positional/`-Path` immediate-child slice with its fixed upstream Unix
default view; it is not provider, drive, wildcard, recurse, link, pipeline, or
general formatting compatibility.
