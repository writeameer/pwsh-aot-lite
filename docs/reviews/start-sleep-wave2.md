# Review: Start-Sleep Wave 2 milliseconds calibration slice

Date: `2026-09-22`
Claim reviewed: `Start-Sleep is executable only for generated -Milliseconds <non-negative invariant int> and generated -ms; Seconds, Duration/ts, positional, and pipeline input remain rejected.`
Upstream commit: `1e53f6bbab4b8791eae782474d21889f9e5d6038`

## Evidence

- Source/provenance: `.upstream/PowerShell/src/Microsoft.PowerShell.Commands.Utility/commands/utility/StartSleepCommand.cs`; generated `GeneratedCmdletPorts.StartSleep`.
- Implementation boundary: `StartSleepCmdlet` consumes only `IAotDelay.DelayMilliseconds(int, CancellationToken)` from the immutable `AotHostSubstrate`. `CancellationTokenDelay` waits on the supplied host token's wait handle; no `Thread.Sleep`, task, timer, scheduler, callback, service locator, or cmdlet-owned token exists.
- Fixture coverage: `31-start-sleep-milliseconds.ps1` and its stock-pwsh parser baseline cover generated name/alias, invalid/range/duplicate/missing forms, and deferred source forms.
- Build and tests: `dotnet build -c Release --no-restore -v:q`, `dotnet run -c Release --no-build -- --self-test`, `Test-ParserReuseGuard.ps1`, parser-baseline verification (31 fixtures), and Phase 10 manifest verification passed. `git diff --check` passed.
- Native AOT evidence: fresh self-contained `osx-arm64` publish to `artifacts/osx-arm64-start-sleep` passed `--self-test`; `Start-Sleep -Milliseconds 0` and `Start-Sleep -ms 1` exit 0 silently, while invalid text rendered source-spanned `AOT3008` and exited 2.

## Independent verdicts

| Persona | PASS / BLOCK | Evidence-backed finding | Resolution / accepted variance |
| --- | --- | --- | --- |
| Upstream Grammar Steward | PASS | Fixture 31 was verified against stock `pwsh` by `Export-PwshParserBaseline.ps1 -Verify` (31 fixtures total), including generated name/alias, malformed/missing/range/duplicate forms, deferred parameter forms, and the structural pipeline form. The working diff changes only the cmdlet adapter, registry, host substrate, metadata/docs, and self-tests; no lexer, parser, AST facade, or lowerer file/path changed. | The existing pinned upstream AST/lowerer preserves the source syntax association. This slice admits execution only after that existing binding path; parser acceptance and lowering policy remain unchanged. |
| Native AOT Boundary Sentinel | PASS | `StartSleepCmdlet` has one injected `IAotDelay` dependency and passes only the validated `int` value plus `AotExecutionContext.StopToken`; it creates no cancellation source, task, timer, callback, scheduler, reflection/runtime-loading surface, or output value. `CancellationTokenDelay` uses the supplied token's existing wait handle, checks cancellation before and after the bounded wait, and cannot manufacture or own a token. The immutable `AotHostSubstrate` composes exactly one local delay implementation. | Independent evidence 2026-09-22: Release build and self-test passed; the patch-only static-surface scan found no `Thread.Sleep`, task, timer, scheduler, or cmdlet-owned `CancellationTokenSource`; a fresh self-contained `osx-arm64` publish in `artifacts/osx-arm64-start-sleep-aot-review` passed `--self-test`, `-Milliseconds 0`, `-ms 1`, and the invalid-input `AOT3008`/exit-2 smoke. The fixed wait seam remains acceptable only for bounded, token-cancellable elapsed waits. |
| Static Data-Plane & Binder Guardian | PASS | `StartSleepCmdlet` derives its command shape only from `GeneratedCmdletPorts.StartSleep.CreateAotDescriptor("Milliseconds")`. The generated `int` scalar preserves the source `Milliseconds` parameter set and generated `ms` alias; no hand-authored parameter, default positional parameter, output record, or `IAotPipelineInputCmdlet` surface was added. The established sole binder therefore rejects positional input with `AOT2005` and every omitted source parameter/alias with `AOT2002`, rather than accepting or reinterpreting it. The adapter then validates its one bound syntax value as an invariant `0..Int32.MaxValue` integer before crossing the host boundary. | Independent evidence 2026-09-22: generated contract inspection confirms `Milliseconds` is scalar `int`, alias `ms`, mandatory in the `Milliseconds` set, and `ByPropertyName` only; direct/pipeline binding is deliberately absent. `AssertStartSleepPort` verifies generated metadata, alias routing, no-output/no-event execution, duplicate/missing/positional/deferred failures, and cancellation. Release build, self-test, parser-reuse guard, all 31 parser baselines, and manifest verification passed. |
| Diagnostic Experience Guardian | PASS | The managed self-test and direct runner checks establish the intended target diagnostic contract: invalid `-Milliseconds nope` renders `AOT3008` at `<command>:1:27`; missing value, duplicate, negative/range, named deferred, positional, and structural-pipeline paths are self-tested with their source spans and stable `AOT2004`/`AOT2003`/`AOT3009`/`AOT2002`/`AOT2005`/`AOT1001` IDs. Successful `-Milliseconds 0` and `-ms 0` produce no records or events; the cancellation fixture verifies no output/error event before the existing exit-130 host contract. | The AOT diagnostic IDs are intentionally target diagnostics, not a claim to reproduce PowerShell `ErrorRecord` IDs. |
| Compatibility Proof Adversary | PASS | The generated contract and upstream source confirm the admitted `Milliseconds` parameter is a mandatory `int` in `0..Int32.MaxValue` with alias `ms`, and the adapter has no output. The corrected port note and `startsleep-seconds-duration-pipeline-deferred` variance now explicitly preserve the boundary: upstream `Milliseconds` `ValueFromPipelineByPropertyName` is rejected with every other pipeline mode before binding; `Seconds`, `Duration`/`ts`, both position-0 modes, and command aliases such as `sleep` are also fail-closed. | The documented accepted differences are complete for this calibration slice: the structural pipeline lowerer replaces all source pipeline binding, and host-token cancellation replaces the source private `ManualResetEvent`/`StopProcessing` synchronization without claiming Ctrl+C wiring. |
| Architecture Guard | PASS | The patch extends the fixed `AotHostSubstrate` composition tuple with exactly one closed capability, `IAotDelay.DelayMilliseconds(int, CancellationToken)`. Its sole local implementation waits on the supplied host token's existing wait handle and checks that token before and after the bounded wait. It exposes no clock reading, deadline, timer, task, callback, scheduler, thread selection, process launch, token creation, registration, service lookup, or runtime registration capability. `StartSleepCmdlet` is the single consumer and has no command-owned lifecycle or ambient host access. | The authority map, bounded-delay policy, cmdlet note, campaign row, and variance ledger all say the same narrow thing: only direct generated `-Milliseconds`/`-ms` waits are admitted, while seconds, duration, positional/pipeline modes, Ctrl+C wiring, and asynchronous/scheduling behavior remain deferred. `rg` confirms `AotHostSubstrate` has one local construction site; `git diff --check` and the full managed/parser/manifest gate set passed. |

## Outcome

`PASS` — the released support claim is only direct generated `-Milliseconds`
and `-ms` waits through the bounded host-owned cancellation seam. No broader
`Start-Sleep` support is authorized by this ledger.
