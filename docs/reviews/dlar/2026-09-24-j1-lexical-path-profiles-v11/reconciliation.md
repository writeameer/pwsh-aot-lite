# Reconciliation and next gate

## Amendment disposition

| Original amendment | Disposition in J1 v11 |
| --- | --- |
| A1 PathText policy | resolved by immutable host dialects, full-string lexical rules, and no-authority boundary |
| A2 P1 surface table | resolved by `Join-Path` admitted/rejected static contract |
| A3 P2 surface table | resolved by `Split-Path` routes, selector sets, outputs, and conflicts |
| A4 diagnostics matrix | resolved by `AOT6210`–`AOT6214`, spans, precedence, and zero-output behavior |
| A5 proof plan | resolved by exact output/span/authority assertions, composition, dialect, and batch fixtures |
| A6 requirement-result schema | resolved by source-pinned list of 12 `requires-substrate` outcomes and their next approval point |

## Decision boundary

The shared safe boundary is direct lexical physical-path text transformation.
The accepted packet authorizes converter Step 2 only: add P1/P2 profiles and
explicit requirement records.  It does not authorize a runtime adapter, source
support claim, or expansion of excluded capabilities.  Those require separate
implementation evidence and reviewer-matrix verdicts.
