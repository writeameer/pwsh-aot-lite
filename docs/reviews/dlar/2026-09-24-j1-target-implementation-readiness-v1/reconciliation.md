# Reconciliation

| Amendment | Disposition |
| --- | --- |
| separate lexical text from physical resolution | required `LexicalPathText.cs` seam |
| preserve positional/remaining groups | required; no flattening/reparse |
| make static set selection explicit | required; conflict is `AOT6213` |
| reject unsupported common/property/dynamic routes | required; shared fail-closed diagnostics |
| synchronize metadata/catalog/help/registry | required; existing equality gate remains authority |
| prove spans, atomicity, and no authority calls | required; counted fixtures and fresh AOT gate |
| preserve AST group identity | accepted; group plan to group syntax argument, no flattening |
| express J1 binding without a second binder | accepted; descriptor-owned `SequentialStatic` inside `AotCmdletRegistry.BindCommand` |
| close actual runner output | accepted; `TextRecord` / `BooleanRecord`, projected only through `PipelineValueAdapter` |
| map groups at every compose slot | accepted; command and attached argument arrays preserve group identity and follow fixed base/child broadcast order |

Independent lenses append any further amendment here. A BLOCK refines this
packet before implementation begins.

Both lenses accepted the final amended packet. The next work is implementation
under the verified coding order, not another design inference pass.
